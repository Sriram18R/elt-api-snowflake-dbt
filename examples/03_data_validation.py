\"\"\"Example 3: Data Validation\n\nThis example demonstrates schema validation and data quality checks.\n\"\"\"\n\nfrom ingestion.main import ELTPipeline\nfrom ingestion.logger import get_logger\n\nlogger = get_logger(__name__)\n\n\ndef main():\n    \"\"\"Run data validation example.\"\"\"\n    try:\n        logger.info(\"Starting data validation example\")\n\n        # Define schema for validation\n        schema = {\n            \"required_fields\": [\"id\", \"email\", \"name\"],\n            \"field_types\": {\n                \"id\": int,\n                \"email\": str,\n                \"name\": str,\n            },\n        }\n\n        with ELTPipeline() as pipeline:\n            # Extract and validate data against schema\n            report = pipeline.run(\n                endpoint=\"api/users\",\n                table_name=\"users\",\n                schema=schema,\n                load_mode=\"append\",\n            )\n\n            logger.info(f\"Validation completed: {report}\")\n            print(f\"\\nData Validation Report:\")\n            print(f\"  Extracted: {report['extracted_records']} records\")\n            print(f\"  Valid: {report['validated_records']} records\")\n            print(f\"  Invalid: {report['extracted_records'] - report['validated_records']} records\")\n            print(f\"  Status: {report['status']}\")\n\n    except Exception as e:\n        logger.error(f\"Data validation failed: {str(e)}\")\n        raise\n\n\nif __name__ == \"__main__\":\n    main()\n